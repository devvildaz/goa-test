// Code generated by goa v3.7.2, DO NOT EDIT.
//
// products HTTP client CLI support package
//
// Command:
// $ goa gen github.com/devvildaz/goa-code/pkg/design

package cli

import (
	"flag"
	"fmt"
	"net/http"
	"os"

	productsc "github.com/devvildaz/goa-code/gen/http/products/client"
	goahttp "goa.design/goa/v3/http"
	goa "goa.design/goa/v3/pkg"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//    command (subcommand1|subcommand2|...)
//
func UsageCommands() string {
	return `products get-all-products
`
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + ` products get-all-products` + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	scheme, host string,
	doer goahttp.Doer,
	enc func(*http.Request) goahttp.Encoder,
	dec func(*http.Response) goahttp.Decoder,
	restore bool,
) (goa.Endpoint, interface{}, error) {
	var (
		productsFlags = flag.NewFlagSet("products", flag.ContinueOnError)

		productsGetAllProductsFlags = flag.NewFlagSet("get-all-products", flag.ExitOnError)
	)
	productsFlags.Usage = productsUsage
	productsGetAllProductsFlags.Usage = productsGetAllProductsUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "products":
			svcf = productsFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "products":
			switch epn {
			case "get-all-products":
				epf = productsGetAllProductsFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     interface{}
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "products":
			c := productsc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "get-all-products":
				endpoint = c.GetAllProducts()
				data = nil
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// productsUsage displays the usage of the products command and its subcommands.
func productsUsage() {
	fmt.Fprintf(os.Stderr, `the product service
Usage:
    %[1]s [globalflags] products COMMAND [flags]

COMMAND:
    get-all-products: GetAllProducts implements get_all_products.

Additional help:
    %[1]s products COMMAND --help
`, os.Args[0])
}
func productsGetAllProductsUsage() {
	fmt.Fprintf(os.Stderr, `%[1]s [flags] products get-all-products

GetAllProducts implements get_all_products.

Example:
    %[1]s products get-all-products
`, os.Args[0])
}
